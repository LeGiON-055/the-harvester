# The Harvester

A Python tool to fetch job listings from various job boards.

## Usage
```bash
python harvester.py --role "Software Engineer" --location "Remote" --output "jobs.csv"
```

## Features
- Fetch listings from Naukri, RemoteOK, and Wellfound.
- Filter by role and location.
- Output to CSV."# the-harvester" 
